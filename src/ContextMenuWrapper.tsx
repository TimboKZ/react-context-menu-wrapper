import React from 'react';

interface ContextMenuWrapperProps {
    id?: string;
}

export const ContextMenuWrapper: React.FC<ContextMenuWrapperProps> = ({ id }) => {
    return <h1>ContextMenuWrapper component</h1>;
};
